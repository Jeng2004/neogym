- name: Build Docker Image
  hosts: 13.60.67.78  # Directly specify server if inventory issues persist
  gather_facts: false
  remote_user: root
  tasks:
    - name: Build the Docker image
      docker_image:
        name: neogym
        tag: latest
        build:
          path: /var/lib/jenkins/workspace/neogym
